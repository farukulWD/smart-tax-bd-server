services:
  app:
    image: ${IMAGE}
    restart: unless-stopped
    env_file: .env
    ports:
      - '5000:5000'
    volumes:
      - uploads:/app/uploads
    # depends_on:        # RabbitMQ not in use
    #   rabbitmq:
    #     condition: service_healthy
    networks: [app-net]

  # rabbitmq:             # RabbitMQ not in use
  #   image: rabbitmq:3.13-management
  #   restart: unless-stopped
  #   environment:
  #     RABBITMQ_DEFAULT_USER: ${RABBITMQ_USER}
  #     RABBITMQ_DEFAULT_PASS: ${RABBITMQ_PASS}
  #   healthcheck:
  #     test: ['CMD', 'rabbitmq-diagnostics', '-q', 'ping']
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5
  #   volumes:
  #     - rabbitmq-data:/var/lib/rabbitmq
  #   networks: [app-net]

volumes:
  uploads:
  # rabbitmq-data:        # RabbitMQ not in use

networks:
  app-net:
